#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: - The first number of type integers
 * @b: - The second number of type integer
 * Return: Always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
